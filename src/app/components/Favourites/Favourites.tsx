"use client";

import React from "react";
import styles from "./favourites.module.scss";

const Favourites = () => {
  return (
    <div className={`${styles.favouritesContainer} py-5`}>
      <p>Favourites</p>
    </div>
  );
};

export default Favourites;
